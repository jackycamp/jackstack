// populated at build time
// you can define placeholders and inject js at build time if you want
// e.g. you could have a placeholder like:
// const pages = __PAGES__;
//
// You could also register some `onClick` handlers here or something.
